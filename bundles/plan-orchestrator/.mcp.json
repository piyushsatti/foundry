{
  "mcpServers": {
    "progress-tracker": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/mcp_server.py"],
      "env": {
        "PROGRESS_TRACKER_DB": "${CLAUDE_PLUGIN_DATA}/progress.db"
      }
    }
  }
}
