{
  "mcpServers": {
    "manifold": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/mcp_server.py"],
      "env": {
        "MANIFOLD_DB": "${CLAUDE_PLUGIN_DATA}/manifold.db"
      }
    }
  }
}
