# foundry

Everything an agent needs for this repo is in [`CLAUDE.md`](CLAUDE.md): the layout, the invariants,
how to run the checks, and the test for what belongs in Foundry.

This file is a pointer on purpose. A second copy of that context would drift out of step with the
first, and then neither could be trusted.
